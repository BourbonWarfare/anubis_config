- name: allow-aar
  action: ALLOW
  expression:
    all:
      - host.startsWith("aar")
      - path.startsWith("/style/") || path.startsWith("/leaflet/") || path.startsWith("/scripts/") || path.startsWith("/data/") || path.startsWith("/images/") || path.startsWith("/api/")
