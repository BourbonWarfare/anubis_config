- name: allow-forums
  action: ALLOW
  expression:
    all:
      - host.startsWith("forums.")
      - any:
        - path.startsWith("/styles/")
        - path.startsWith("/ext/")
        - path.startsWith("/assets/")
